# WebGUI UX 提案 — 决策日志

原型评审中已确认(或待确认)的决定及约束。落地时以本文件为清单,逐项进入实现。

## 已确认

| # | 决定 | 约束 / 备注 |
| --- | --- | --- |
| D1 | 原型采用总览页 + hash 子页路由,深色解说层 / 浅色 App 层分层 | 仅原型结构,不代表产品要有 hub |
| D2 | App 界面默认以**亮色主题**绘制评审 | 产品仍保留双主题;落地需两套 tokens |
| D3 | protected 文案改为 **"can't compress"**(生产 UI 现为 "not compressible") | 落地同步:`review-webui.html` 的 `.tag.lock` 文案、帮助文案、`skills/midcompact/` 文档;user 消息为可压缩 atom(原型曾误标) |
| D4 | 投影条采用**两段式**:实心=commit 后(点估计),斜纹=被释放;±15% 不确定度只出现在数字与悬停说明 | 视觉层不做概率表达(三段式被否) |
| D5 | 假数据采用真实量级：200k 窗口、now 80% → after ≈26–40% | 原型演示值；±15% 与换算比例（0.75/0.25/1.1k）均为 hard-code 占位，**不得带入正式稿**。落地方法论：误差带由字符分类比例区间按内容构成计算（ASCII ≈±10–15%，CJK ≈±25–35%），非对称常数；比例表写入假设文档；可选二阶段：用 Pi 上报的 commit 前后 usage 差值做自校准 |

## 已确认方向(细节待实现评审)

| # | 决定 | 备注 |
| --- | --- | --- |
| P1 | 决策条替代四张指标卡 | 数字:ranges / coverage / −chars% / pending + 投影条 |
| P2 | Timeline Raw ⇄ Projected 切换 | 投影卡:正常 / pending 警告两种形态 |
| P3 | 原文按需查看 | 新增 `GET /api/atom/:ref`(服务端已有 fullText) |
| P4 | pending 三处前置(列表 / 投影卡 / 关闭对话框) | 关闭对话框含 "Fix pending" 跳转 |
| P5 | reviewed 勾选 + 吸底操作行 + "回终端 commit" 指引 | reviewed 态为客户端态,需定持久化位置 |
| P6 | Selection 三态 → 两态(选中=压缩,未选=保留) | 需确认服务端 keepRefs 是否随之简化 |
| P7 | Selection 键盘圈选(↑↓ / Space / G) | 需同步帮助文案 |
| P8 | 契约修订:允许"展示级"token 估算 | est. 标注 + 不确定度;无否决权,commit 门禁仍基于事实;SPEC/README 同步 |

## 进行中

- **V1 视觉风格**:现状暗色 vs shadcn 风亮/暗(`prototype-visual.html`)。倾向:采纳 shadcn 的纪律(中性表面、1px 边框、语义色克制),保留"数据用 mono"的标识;accent 取舍待评审。已加 D 变体(B 同配色 + 宽松密度:基准 14px、辅助 ≥12px、内距 +30~40%),检验"字小且挤"假设。后继反馈:时间线灰墙易疲劳,加 E 变体(=D+视觉锚点:组左色轨、user 地标、kind 低饱和着色、体积条、对比度分层)。已确认 E;另:绿色削减标注统一加粗(两原型同步)。

## 明确不做

- 浏览器内 commit 按钮(人工门禁留在终端)
- 估算参与门禁/决策逻辑
- remove 撤销(需新服务端契约,收益不匹配)
